import express from 'express';
import controller from '../controllers/stock';
const router = express.Router();

router.get('/posts', controller.getstockinfo);
router.get('/posts/date', controller.getstockinfobydate);

export = router;
